import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DynamicFormField from "./DynamicFormField.stories.tsx";

<Meta of={DynamicFormField} title="DynamicFormField" />

# DynamicFormField

Dynamic Form Field component with different props.

#### Input text

<Canvas of={DynamicFormField.InputField} />

## Usage

```ts
import React, { FC } from "react";
import { FormProvider } from "../../providers/FormProvider";
import { FormField } from "../../types";
import DynamicFormField from "../DynamicFormField";

const fieldData: FormField = {
  fieldId: "exampleField",
  label: "Example Field",
  type: "text",
  placeholder: "Enter text here",
};
const Example: FC<FormField> = (field = fieldData) => {
  return (
    <FormProvider
      formSchema={{ formId: "example", title: "example", fields: [field] }}
    >
      <DynamicFormField {...field} />
    </FormProvider>
  );
};

export default Example;

```

#### Common Arguments

- **fieldId** _`string`_ - The unique identifier for the field. This is used to manage the field state and handle validation.
- **label** _`string`_ - The label for the field. This is displayed to the user as a prompt for what to enter in the field.
- **type** _`string`_ - The type of the field. This determines the input type (e.g., text, number, email, select, radio, etc.).
- **placeholder** _`string`_ - The placeholder text for the field. This is displayed when the field is empty and provides a hint to the user about what to enter.
- **value** _`string`_ - The current value of the field. This is used to manage the field state and handle validation.
- **options** _`Array<{ label: string; value: string }> or Array<string>`_ - The options for select and radio fields. This is used to populate the dropdown or radio button group with available choices.
- **required** _`boolean`_ - Whether the field is required or not. This is used to manage validation and ensure that the user provides a value for the field before submitting the form.
- **disabled** _`boolean`_ - Whether the field is disabled or not. This is used to prevent user interaction with the field.
- **styles** _`object`_ - The styles for the field. This is used to customize the appearance of the field and make it consistent with the overall design of the form.
- **className** _`string`_ - The class name for the field. This is used to apply custom styles to the field and make it consistent with the overall design of the form.
{/* - **onChange** _`function`_ - The function to call when the field value changes. This is used to manage the field state and handle validation. */}
{/* - **onBlur** _`function`_ - The function to call when the field loses focus. This is used to manage the field state and handle validation. */}
{/* - **onFocus** _`function`_ - The function to call when the field gains focus. This is used to manage the field state and handle validation. */}
{/* - **onKeyDown** _`function`_ - The function to call when a key is pressed in the field. This is used to manage the field state and handle validation. */}
{/* - **onKeyUp** _`function`_ - The function to call when a key is released in the field. This is used to manage the field state and handle validation. */}
{/* - **onKeyPress** _`function`_ - The function to call when a key is pressed in the field. This is used to manage the field state and handle validation. */}
{/* - **onClick** _`function`_ - The function to call when the field is clicked. This is used to manage the field state and handle validation. */}
- **containerClassName** _`string`_ - The class name for the field container. This is used to apply custom styles to the field container and make it consistent with the overall design of the form.
- **containerStyles** _`object`_ - The styles for the field container. This is used to customize the appearance of the field container and make it consistent with the overall design of the form.
- **labelClassName** _`string`_ - The class name for the field label. This is used to apply custom styles to the field label and make it consistent with the overall design of the form.
- **labelStyles** _`object`_ - The styles for the field label. This is used to customize the appearance of the field label and make it consistent with the overall design of the form.

#### Select field
<Canvas of={DynamicFormField.SelectField} />

#### Radio field
<Canvas of={DynamicFormField.RadioField} />

#### Checkbox field
<Canvas of={DynamicFormField.CheckboxField} />

{/* #### Textarea field
<Canvas of={DynamicFormField.TextareaField} /> */}

#### Datepicker field
<Canvas of={DynamicFormField.DatepickerField} />

#### Group field
<Canvas of={DynamicFormField.GroupField} />